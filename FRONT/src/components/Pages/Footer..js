import React from "react";

const Footer = () => {
  return (
    <div
      style={{
        backgroundImage:
          'url("E:InterviewDashboard-in-React/src/assets/Ev.jpeg")',
        backgroundSize: "cover",
        color: "white",
        padding: "20px",
      }}
    >
      <p style={{ fontSize: "18px", textAlign: "center" }}>
        Enable knowledge workers to be 100x productive through spending less
        time on collaboration and getting work done through generative AI
      </p>
    </div>
  );
};

export default Footer;
